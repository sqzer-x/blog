# Keeps the SHA-pinned actions in .github/workflows current. A pin that is never bumped
# stops receiving the action's fixes, so each release arrives as a pull request instead.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns: ['*']
